public enum GameMode {
    /** Each player on their own board, race through all 3 levels — fastest total time wins. */
    RACE,
    /** All players share one map and fight for the same pellets — highest score wins. */
    BATTLE
}
